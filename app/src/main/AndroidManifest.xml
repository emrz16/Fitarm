<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitarm"
        tools:targetApi="31">
        <activity
            android:name=".RepeticionAlarmaActivity"
            android:exported="false"
            android:label="@string/title_activity_repeticion_alarma"
            android:theme="@style/Theme.Fitarm" />
        <activity
            android:name=".CrearAlarmaActivity"
            android:exported="false"
            android:label="@string/title_activity_crear_alarma"
            android:theme="@style/Theme.Fitarm" />
        <activity
            android:name=".ListaAlarmas"
            android:exported="false"
            android:label="@string/title_activity_lista_alarmas"
            android:theme="@style/Theme.Fitarm" />
        <activity
            android:name=".SonidoAlarmaActivity"
            android:exported="false"
            android:label="SonidoAlarmaActivity"
            android:theme="@style/Theme.Fitarm" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Fitarm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>